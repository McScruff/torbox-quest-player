You will need your Torbox API

Sideload app (only tested on Quest 2), launch, type your api (or when you need to type adb input with adb shell input text "your API" )

I use 4XVR as an external player, you pick your media and it opens in external player

